O listSections pode ser um outro visitor?
  Se usarmos visitor entao temos de imprimir apenas as secçoes, podemos deixar o outro com apenas new(), return new()
chamar novo visitor dentro de visitor?
    no metodo visitSection do visitor_1:
          element.accept(complete_diferent_visitor)
    valido??

in getSection
  if (size == 0 || id > size || id < 0 )

se ja faco a associacao entre uniqID e TextElement no hashmap do document continuo a precisar de guardar o unicID no TextElement??
