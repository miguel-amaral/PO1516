O listSections pode ser um outro visitor?
  Se usarmos visitor entao temos de imprimir apenas as secçoes, podemos deixar o outro com apenas new(), return new()
chamar novo visitor dentro de visitor?
    no metodo visitSection do visitor_1:
          element.accept(complete_diferent_visitor)
    valido??

in getSection
  if (size == 0 || id > size || id < 0 )

Descontados quando usamos string[]??

quando vai ser feita sessao de C++ e de BASH






removeSection:
  //FIXME quem ve filhos?
  //FIXME remover unicID do _document, pode ser no TEXTUI? isto e chamar funcao do document que elemina


TextElement:
  Posso ter set uniqueId = null
  Antes de imprimir chamar o hasUnicId
    caso true imprimir
    else imprimir ""
Fazer politica identica para fileName do document
